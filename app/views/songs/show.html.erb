<h1>Song: <%= @song.name %></h1><br>
<h1>Artist: <%= link_to @song.artist.name, artist_path(@song.artist) if @song.artist %></h1><br>
<h1>Genre: <%= link_to @song.genre.name, genre_path(@song.genre) if @song.genre %></h1>
